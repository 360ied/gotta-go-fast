# Fallback XDG_CACHE_HOME & CABAL_DIR to /tmp if ~/.cache is read-only
if ! touch "${XDG_CACHE_HOME:-$HOME/.cache}/.writable_test" 2>/dev/null; then
  export XDG_CACHE_HOME="/tmp/cache"
  export CABAL_DIR="/tmp/cabal"
fi
rm -f "${XDG_CACHE_HOME:-$HOME/.cache}/.writable_test" 2>/dev/null

use flake
